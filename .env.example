# Gemini API key for all apps (Render, Frame-variator, CinePrompt, POV, etc.)
# Copy this file to .env.local and set your key. Then rebuild: npm run build --prefix render && npm run build --prefix frame-variator
# GEMINI_API_KEY=your_key_here
