# Gemini API key for all apps (cineprompt, pov, chronos, swag, avatar, render, frame-variator, connect).
# All apps read from this file when building; set GEMINI_API_KEY only here. Then: npm run build:apps
# GEMINI_API_KEY=your_key_here
